{
  "name": "nano-claude",
  "version": "2.0.0",
  "description": "Real local LLM — runs entirely in browser, no API keys, no payment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": { "node": ">=18.0.0" },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "uuid": "^11.0.3"
  }
}
